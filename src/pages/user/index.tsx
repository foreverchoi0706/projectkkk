import { Input, Layout } from "antd";

const User = () => {
  return (
    <Layout
      style={{
        maxWidth: "600px",
        margin: "0 auto",
        height: "100vh",
      }}
    >
      <Input />
    </Layout>
  );
};

export default User;
