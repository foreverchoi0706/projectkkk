import { Typography } from "antd";
import { FC } from "react";

const Page: FC = () => {
  return (
    <main>
      <Typography className="text-sm">찜한 아이템 0</Typography>
    </main>
  );
};

export default Page;
